#!/bin/bash

# Check if the DECIMAL environment variable is set
if [ -z "$DECIMAL" ]; then
    echo "ERROR: The DECIMAL environment variable is not set."
    exit 1
fi

# Use 'printf' to convert and display the decimal number in hexadecimal
printf "%X\n" "$DECIMAL"

