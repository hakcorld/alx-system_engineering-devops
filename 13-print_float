#!/bin/bash

# Check if the NUM environment variable is set
if [ -z "$NUM" ]; then
    echo "ERROR: The NUM environment variable is not set."
    exit 1
fi

# Print the number with two decimal places
printf "%.2f\n" "$NUM"

